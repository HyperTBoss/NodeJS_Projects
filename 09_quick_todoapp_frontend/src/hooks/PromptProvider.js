import { createContext } from "react";
import React, {useState, useEffect} from 'react'
import LocalStorage from '../LocalStorage'


function usePrompt(ReactComponent, backgroundComponent){

}
function useTodoPrompt(ReactComponent, backgroundComponent){
  
}
function removePrompt(keyName, initialValue){
  
}

export const PromptProvider = createContext({});
